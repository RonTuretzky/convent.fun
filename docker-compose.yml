services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: convent
      POSTGRES_USER: convent
      POSTGRES_PASSWORD: convent
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
